# Dynamic Diagram Flow — VENHAN React Assignment

## 🧠 Objective
Build a React application that dynamically generates and manipulates an interactive diagram using **React Flow**.  
Users can add, edit, and delete nodes and edges based on JSON metadata, and visualize relationships interactively.

---

## ⚙️ Tech Stack
- **React (v18+)**
- **React Flow** for diagram rendering
- **React Hooks (useState, useEffect)**
- **UUID** for unique IDs
- **LocalStorage** for persistence

---

## 🚀 Setup & Run Locally

1. Clone or download the repository:
   ```bash
   git clone https://github.com/krishnamohanreddy16/dynamic-diagram-flow
   cd dynamic-diagram-flow
